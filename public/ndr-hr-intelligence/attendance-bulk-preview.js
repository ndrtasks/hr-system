(()=>{
'use strict';
const API='https://ecaexxjfzujoesptzurd.supabase.co/functions/v1/ndr-attendance-bulk';
const q=id=>document.getElementById(id);
const token=()=>window.NDROdooVault?.token||localStorage.getItem('ndr-connector-token')||'';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
let previewSig='',previewData=null;
function notify(msg){try{if(typeof toast==='function')return toast(msg)}catch{}alert(msg)}
async function post(action,body){const t=token();if(!t)throw new Error('اتصال Odoo غير جاهز');const r=await fetch(`${API}?action=${encodeURIComponent(action)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({launchToken:t,...body})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||'تعذر تنفيذ العملية');return d}
function dates(){const a=q('attBulkFrom')?.value,b=q('attBulkTo')?.value;if(!a||!b||a>b)return[];const allowed=[...document.querySelectorAll('[data-bulk-day]:checked')].map(x=>Number(x.dataset.bulkDay));const out=[];for(let d=new Date(a+'T12:00:00Z'),end=new Date(b+'T12:00:00Z');d<=end&&out.length<100;d=new Date(d.getTime()+86400000)){if(allowed.includes(d.getUTCDay()))out.push(d.toISOString().slice(0,10))}return out}
function employees(){return[...document.querySelectorAll('[data-bulk-emp]:checked')].map(x=>Number(x.value))}
function payload(){return{employeeIds:employees(),dates:dates(),checkIn:q('attBulkIn')?.value||'07:30',checkOut:q('attBulkOut')?.value||'15:30',reason:(q('attBulkReason')?.value||'').trim()}}
function signature(p){return JSON.stringify([p.employeeIds,p.dates,p.checkIn,p.checkOut,p.reason])}
function invalidate(){previewSig='';previewData=null;const commit=q('attBulkCommit');if(commit)commit.style.display='none';const panel=q('attBulkServerPreview');if(panel){panel.classList.remove('show');panel.innerHTML=''}const b=q('attBulkPreviewAction');if(b){b.disabled=false;b.textContent='معاينة قبل الإضافة'}}
function row(x,label){return `<div class="abp-row"><span><b>${esc(x.employeeName||'موظف')}</b><small>${esc(x.date||'')}</small></span><em>${esc(label)}</em></div>`}
function render(d){const panel=q('attBulkServerPreview');if(!panel)return;const p=d.preview||{},skips=[...(p.skippedLeave||[]).map(x=>[x,`إجازة${x.leaveType?' • '+x.leaveType:''}`]),...(p.skippedExisting||[]).map(x=>[x,'بصمة موجودة']),...(p.skippedFuture||[]).map(x=>[x,'تاريخ مستقبلي'])];panel.innerHTML=`<div class="abp-counts"><div><span>سيضاف</span><b>${d.willCreate||0}</b></div><div><span>إجازات</span><b>${d.skippedLeave||0}</b></div><div><span>بصمة موجودة</span><b>${d.skippedExisting||0}</b></div><div><span>مستقبلي</span><b>${d.skippedFuture||0}</b></div></div><div class="abp-note">هذه معاينة مباشرة من Odoo ولم يتم حفظ أي شيء بعد.${d.overnight?' الدوام ليلي والخروج محسوب في اليوم التالي.':''}</div>${skips.length?`<details><summary>عرض الأيام التي سيتم تخطيها (${skips.length})</summary><div class="abp-list">${skips.map(([x,l])=>row(x,l)).join('')}</div></details>`:'<div class="abp-ok">لا توجد تعارضات أو إجازات أو بصمات سابقة ضمن الاختيار.</div>'}`;panel.classList.add('show')}
async function preview(){const p=payload();if(!p.employeeIds.length)return notify('اختر موظفا واحدا على الأقل');if(!p.dates.length)return notify('اختر الفترة وأيام العمل');if(!p.reason)return notify('اكتب سبب الإضافة الجماعية');const b=q('attBulkPreviewAction');b.disabled=true;b.textContent='جار فحص Odoo…';try{const d=await post('preview',p);previewData=d;previewSig=signature(p);render(d);q('attBulkCommit').style.display='';q('attBulkCommit').disabled=!(d.willCreate>0);q('attBulkCommit').textContent=d.willCreate>0?`اعتماد وإضافة ${d.willCreate} سجل إلى Odoo`:'لا توجد سجلات جديدة للإضافة';b.textContent='إعادة المعاينة'}catch(e){notify(e.message||String(e));b.textContent='معاينة قبل الإضافة'}finally{b.disabled=false}}
async function commit(){const p=payload();if(!previewData||previewSig!==signature(p))return notify('تغير الاختيار. شغل المعاينة مرة أخرى قبل الحفظ');if(!(previewData.willCreate>0))return;const n=previewData.willCreate;if(!confirm(`تم التحقق من Odoo. سيتم إضافة ${n} سجل فقط، مع تخطي الإجازات والبصمات الموجودة. اعتماد؟`))return;const b=q('attBulkCommit');b.disabled=true;b.textContent='جار الحفظ في Odoo…';try{const d=await post('create',p);const r=q('attBulkResult');if(r){r.innerHTML=`<b>${esc(d.message||'تمت العملية')}</b><br>أضيف: ${d.created||0} • تخطي إجازة: ${d.skippedLeave||0} • بصمة موجودة: ${d.skippedExisting||0} • مستقبلي: ${d.skippedFuture||0}`;r.classList.add('show')}notify(`تمت إضافة ${d.created||0} سجل إلى Odoo`);q('attReload')?.click();invalidate()}catch(e){notify(e.message||String(e))}finally{b.disabled=false}}
function style(){if(q('abpStyle'))return;const s=document.createElement('style');s.id='abpStyle';s.textContent=`.abp-panel{display:none;margin:0 20px 14px;border:1px solid rgba(120,199,183,.14);border-radius:12px;background:#091820;padding:12px}.abp-panel.show{display:block}.abp-counts{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.abp-counts>div{background:#0d202a;border:1px solid rgba(255,255,255,.06);border-radius:9px;padding:9px}.abp-counts span{display:block;font-size:8px;color:#738a96}.abp-counts b{display:block;margin-top:3px;font-size:18px;color:#e8f0f2}.abp-note{font-size:8.5px;color:#8ea1aa;margin-top:9px}.abp-ok{font-size:9px;color:#9bcdbd;margin-top:10px}.abp-panel details{margin-top:9px}.abp-panel summary{cursor:pointer;font-size:9px;color:#d0dde1}.abp-list{max-height:170px;overflow:auto;margin-top:7px;border-top:1px solid rgba(255,255,255,.05)}.abp-row{display:flex;justify-content:space-between;gap:10px;padding:7px 2px;border-bottom:1px solid rgba(255,255,255,.04)}.abp-row b{font-size:8.5px}.abp-row small{display:block;color:#718691;font-size:7.5px}.abp-row em{font-style:normal;color:#d4b878;font-size:8px}.abp-actions{display:flex;gap:8px;margin-inline-start:auto}@media(max-width:700px){.abp-counts{grid-template-columns:1fr 1fr}.abp-actions{width:100%}.abp-actions .att-btn{flex:1}}`;document.head.appendChild(s)}
function enhance(){if(q('attBulkPreviewAction'))return;const old=q('attBulkSave'),foot=old?.parentElement;if(!old||!foot)return;style();const clone=old.cloneNode(true);clone.id='attBulkPreviewAction';clone.textContent='معاينة قبل الإضافة';old.replaceWith(clone);const commitBtn=document.createElement('button');commitBtn.id='attBulkCommit';commitBtn.className='att-btn primary';commitBtn.style.display='none';commitBtn.textContent='اعتماد وإضافة إلى Odoo';const wrap=document.createElement('div');wrap.className='abp-actions';clone.parentNode.insertBefore(wrap,clone);wrap.appendChild(clone);wrap.appendChild(commitBtn);const panel=document.createElement('div');panel.id='attBulkServerPreview';panel.className='abp-panel';foot.parentElement.insertBefore(panel,foot);clone.addEventListener('click',preview);commitBtn.addEventListener('click',commit);q('attBulkBack')?.addEventListener('change',invalidate);q('attBulkBack')?.addEventListener('input',e=>{if(e.target.matches('#attBulkIn,#attBulkOut,#attBulkReason,#attBulkFrom,#attBulkTo'))invalidate()})}
async function boot(){for(let i=0;i<120;i++){if(q('attBulkSave')&&q('attBulkBack'))break;await new Promise(r=>setTimeout(r,80))}enhance()}
boot();
})();