import {readFile,writeFile} from 'node:fs/promises';
const P='public/ndr-hr-v2/app.js';let s=await readFile(P,'utf8');
function exact(src,a,b,label){const n=src.split(a).length-1;if(n!==1)throw new Error(label+': expected 1 match, got '+n);return src.replace(a,b)}
function fn(src,name,next,body){const a=src.indexOf('function '+name+'('),b=src.indexOf(next,a);if(a<0||b<0)throw new Error('function patch failed: '+name);return src.slice(0,a)+body+'\n'+src.slice(b)}

s=exact(s,"N('days','أيام الإجازة الحالية','45')],calc:calcSick","N('days','أيام الإجازة الحالية','45'),S('kind','نوع الحالة',[['ordinary','مرض عادي'],['work','إصابة عمل أو مرض مهني']])],calc:calcSick",'sick type');
s=fn(s,'calcSick','function calcOT(',"function sickAlloc(used,n,w){let alloc=[0,0,0],pay=0,start=0;for(const [i,len,rate] of [[0,30,1],[1,60,.75],[2,30,0]]){const end=start+len,from=Math.max(used,start),to=Math.min(used+n,end),x=Math.max(0,to-from);alloc[i]=x;pay+=x*(w/30)*rate;start=end}return{alloc,pay,beyond:Math.max(0,used+n-120)}}\nfunction calcSick(v){if(v.kind==='work')return fail('إصابة العمل أو المرض المهني لا يحسبان بشرائح المادة 117 العادية؛ استخدم منافع فرع الأخطار المهنية لدى التأمينات.');const first=isoDate(v.first),st=isoDate(v.current),w=+v.wage,used=+v.used,n=+v.days;if(!first||!st||st<first||w<=0||used<0||n<=0)return fail('تحقق من بداية الدورة المرضية وتاريخ الإجازة والأيام والأجر.');let cur=st,remain=n,isFirst=true,totalPay=0,total=[0,0,0],beyond=0,cycles=0,guard=0,firstCycle=null,lastCycle=null;while(remain>0&&guard++<50){const cyc=legalCycle(first,cur);if(!cyc||!cyc.end)return fail('تعذر تحديد دورة السنة المرضية.');if(!firstCycle)firstCycle=cyc;lastCycle=cyc;const span=Math.max(1,daysBetween(cur,cyc.end)),seg=Math.min(remain,span),u=isFirst?used:0,r=sickAlloc(u,seg,w);totalPay+=r.pay;total=total.map((x,i)=>x+r.alloc[i]);beyond+=r.beyond;remain-=seg;cur=addDays(cur,seg);cycles++;isFirst=false}if(remain>0)return fail('مدة الإجازة المدخلة تتجاوز نطاق المعالجة الآمنة.');renderResult({label:'أجر الإجازة المرضية الحالية',total:money(totalPay),rows:[['أساس السنة المرضية',basisLabel()],['بداية أول دورة مستخدمة',fmtDate(firstCycle.start)],['نهاية آخر دورة مستخدمة',fmtDate(lastCycle.end)],['عدد الدورات',String(cycles)],['أيام سابقة في الدورة الأولى',used+' يوم'],['بأجر كامل',total[0]+' يوم'],['بـ75% من الأجر',total[1]+' يوم'],['دون أجر',total[2]+' يوم'],['فوق 120 يوما داخل أي دورة',beyond+' يوم']],note:'إذا عبرت الإجازة ذكرى بدء السنة المرضية يبدأ المحرك شرائح جديدة للدورة التالية. وإذا تداخلت المرضية مع السنوية تتوقف السنوية ثم تستأنف، ولا يعوض يوم الراحة الأسبوعية الواقع داخل المرضية.',type:beyond?'warn':'',sources:['conditions','general']})}");

s=exact(s,"N('usedCompDays','أيام الإجازة التعويضية المستخدمة خلال السنة','0')],calc:calcOT","N('usedCompDays','أيام الإجازة التعويضية المستخدمة خلال السنة','0'),N('annualHoursUsed','ساعات إضافية سابقة خلال السنة','0'),S('beyond720Consent','موافقة العامل إذا تجاوز الإجمالي 720 ساعة',[['no','لا / غير موجودة'],['yes','نعم']]),D('otDate','تاريخ العمل الإضافي — للإجازة التعويضية','date',today()),D('leaveDate','تاريخ التمتع المتوقع بالإجازة التعويضية','date',today()),S('laterLeaveAgreement','اتفاق على التمتع بعد 60 يوما',[['no','لا'],['yes','نعم']])],calc:calcOT",'OT edge fields');
s=fn(s,'calcOT','function calc77(',"function calcOT(v){const b=+v.basic,a=+v.actual,h=+v.hours,m=+v.monthlyHours,prev=+v.annualHoursUsed;if(b<=0||a<=0||h<0||m<=0||prev<0)return fail('تحقق من الأجر الأساسي والفعلي والساعات.');const annual=prev+h;if(annual>720&&v.beyond720Consent!=='yes')return fail('إجمالي العمل الإضافي يتجاوز 720 ساعة في السنة؛ يلزم موافقة العامل قبل اعتماد التجاوز.');if(v.settle==='leave'){const ratio=+v.leaveRatio,dh=+v.dailyHours,used=+v.usedCompDays;if(ratio<1.5||dh<=0||used<0)return fail('الإجازة التعويضية تتطلب 1.5 ساعة على الأقل لكل ساعة إضافية.');const od=isoDate(v.otDate),ld=isoDate(v.leaveDate);if(!od||!ld||ld<od)return fail('حدد تاريخ العمل الإضافي وتاريخ التمتع المتوقع بصورة صحيحة.');const gap=daysBetween(od,ld);if(gap>60&&v.laterLeaveAgreement!=='yes')return fail('تاريخ الإجازة التعويضية يتجاوز 60 يوما من العمل الإضافي ولا يوجد اتفاق على خلاف ذلك.');const lh=h*ratio,days=lh/dh,totalDays=used+days;if(totalDays>30)return fail('الإجازة التعويضية الناتجة ترفع المستخدم خلال السنة فوق سقف 30 يوما.');return renderResult({label:'الإجازة التعويضية',total:dec(lh,2)+' ساعة',rows:[['ساعات العمل الإضافي',dec(h,2)],['إجمالي ساعات السنة',dec(annual,2)],['معامل التعويض',dec(ratio,2)],['أيام الإجازة الناتجة',dec(days,2)],['إجمالي أيام السنة',dec(totalDays,2)],['الفاصل حتى التمتع',gap+' يوم']],note:'الرصيد غير المستخدم عند انتهاء العلاقة يسوى بأجره. تجاوز 720 ساعة ظهر فقط عند وجود موافقة العامل حسب الإدخال.',sources:['conditions']})}const bh=b/m,ah=a/m,rate=ah+.5*bh,total=rate*h;renderResult({label:'أجر العمل الإضافي',total:money(total),rows:[['أجر الساعة من الأجر الفعلي',money(ah)],['50% من أجر الساعة الأساسي',money(.5*bh)],['أجر الساعة الإضافية',money(rate)],['عدد الساعات',dec(h,2)],['إجمالي ساعات السنة بعد الطلب',dec(annual,2)]],note:annual>720?'تم تجاوز 720 ساعة بناء على إثبات موافقة العامل في الإدخال.':'ضمن الحد العام للساعات الإضافية السنوية.',sources:['conditions']})}");

s=exact(s,"S('repeatRule','سبب جواز التجربة الجديدة',[['none','لا يوجد'],['different','مهنة أو عمل مختلف'],['gap6','مضى 6 أشهر فأكثر على انتهاء العلاقة السابقة']])]","S('repeatRule','سبب جواز التجربة الجديدة',[['none','لا يوجد'],['different','مهنة أو عمل مختلف'],['gap6','مضى 6 أشهر فأكثر على انتهاء العلاقة السابقة']]),S('contractClause','هل نص العقد صراحة على فترة التجربة ومدتها؟',[['yes','نعم'],['no','لا']]),S('repeatWritten','عند إعادة التجربة: هل يوجد اتفاق مكتوب؟',[['yes','نعم'],['no','لا']])]",'probation formal fields');
s=fn(s,'calcProbation','function calcSettlement(',"function calcProbation(v){const st=isoDate(v.start),n=+v.days,x=+v.excluded;if(!st||n<0||x<0)return fail('تحقق من البيانات.');if(v.contractClause!=='yes')return fail('فترة التجربة يجب النص عليها صراحة في عقد العمل وتحديد مدتها بوضوح.');if(n>180)return fail('مدة التجربة المحتسبة تتجاوز 180 يوما.');if(v.prior==='yes'&&(v.repeatRule==='none'||v.repeatWritten!=='yes'))return fail('إعادة التجربة تحتاج اتفاقا مكتوبا ومهنة/عمل مختلفا أو فاصل 6 أشهر فأكثر.');const end=addDays(st,n+x);renderResult({label:'نهاية فترة التجربة التقديرية',total:fmtDate(end),rows:[['الأيام المحتسبة',n+' يوم'],['الأيام المستبعدة',x+' يوم'],['إجمالي الامتداد',n+x+' يوم']],note:'لا تدخل إجازتا العيدين واليوم الوطني ويوم التأسيس والإجازة المرضية. إذا ظهرت أيام مستبعدة جديدة بسبب الامتداد فأعد احتسابها. إنهاء العقد صحيحا أثناء التجربة لا يرتب تعويضا ولا مكافأة نهاية خدمة.',sources:['labor']})}");

new Function(s);for(const m of ["v.kind==='work'",'annual>720',"gap>60",'totalDays>30',"v.contractClause!=='yes'","v.repeatWritten!=='yes'"])if(!s.includes(m))throw new Error('missing labor extra marker: '+m);await writeFile(P,s,'utf8');console.log('NDR HR labor extra hardening complete');